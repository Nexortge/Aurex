# Gradle JVM tuning. 2GB is plenty for this tiny project.
org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8

# Faster builds — run subprojects in parallel and cache task outputs.
org.gradle.parallel=true
org.gradle.caching=true
